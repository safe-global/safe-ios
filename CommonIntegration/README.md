# CommonIntegration app

This app exists to make integration tests possible. For example, `CommonImplementationsTests` target uses this app as a test host, 
so that Keychain-based `SecureStore` could be tested.
